import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/providers/theme-provider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Curator of Awareness - Exploring Simulation Theory & The Nature of Reality",
  description: "A comprehensive portal exploring simulation theory, consciousness, and the nature of reality. Discover ancient wisdom, modern physics, spiritual perspectives, and the works of leading thinkers on the question: Are we living in a simulation?",
  keywords: "simulation theory, consciousness, reality, Plato's cave, quantum physics, spiritual awakening, Nick Bostrom, Donald Hoffman, simulation hypothesis",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider>{children}</ThemeProvider>
      </body>
    </html>
  );
}
